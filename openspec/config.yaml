# openspec/config.yaml
# SDD configuration for rag-pdf
# Auto-generated by sdd-init on 2026-06-18. Edit to match project evolution.
schema: spec-driven

context: |
  Project: rag-pdf
  Product intent: RAG system — ingests PDF/text into knowledge base, answers
    customer questions, and schedules appointments via Google Calendar.
  First business domain: appointment scheduling for a chiropractor.
  Stack: GREENFIELD — no code exists yet; stack is undecided.
  Architecture: TBD; expected hexagonal/clean with domain separation.
  Testing: No test runner detected (greenfield). Strict TDD disabled until
    a test runner is installed and configured.
  Persistence: hybrid (OpenSpec + Engram).
  Chained PR strategy: auto (review budget 400 changed lines).

strict_tdd: false
strict_tdd_reason: >
  Greenfield project — no test runner installed or configured yet.
  Set strict_tdd to true and add test_command once a test framework is chosen.

testing:
  runner: null
  test_command: ""
  layers:
    unit:
      available: false
      tool: null
    integration:
      available: false
      tool: null
    e2e:
      available: false
      tool: null
  coverage:
    available: false
    command: ""
    threshold: 0
  quality:
    linter:
      available: false
      command: ""
    type_checker:
      available: false
      command: ""
    formatter:
      available: false
      command: ""

rules:
  proposal:
    - Include rollback plan for risky changes
    - Identify which RAG domain is affected (knowledge-ingestion, qa-answering, calendar-scheduling)
    - State Google Calendar API scope requirements explicitly when calendar features are involved
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
    - Reference domain boundaries (ingestion / QA / scheduling)
  design:
    - Include sequence diagrams for all external API calls (embedding provider, LLM, Google Calendar)
    - Document architecture decisions with explicit rationale
    - Separate RAG pipeline stages (chunking, embedding, retrieval, generation) in diagrams
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
    - Flag tasks requiring API keys or external service access
  apply:
    tdd: false
    test_command: ""
    notes: >
      Update tdd and test_command once a test framework is selected.
      Follow existing code patterns once the stack is decided.
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
    notes: >
      Populate test_command and build_command after stack decision.
  archive:
    - Warn before merging destructive deltas
    - Record LLM model versions and embedding model versions used in each change

sdd_phases:
  - explore
  - propose
  - spec
  - design
  - tasks
  - apply
  - verify
  - archive
